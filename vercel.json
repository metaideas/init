{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run build --filter=web",
  "framework": "astro",
  "installCommand": "bun install --frozen-lockfile && bun run codegen --filter=web",
  "outputDirectory": "apps/web/dist"
}
