import { createUrlBuilder } from "{{utilsPackage}}/url"
import { ENV } from "#shared/env.generated.ts"

const isProduction = import.meta.env.PROD

export const buildApiUrl = createUrlBuilder(
  ENV.PUBLIC_API_URL,
  isProduction ? "https" : "http"
)
