import { createClient } from "{{apiPackage}}/client"
import { getAuthHeaders } from "#shared/auth.ts"
import { buildApiUrl } from "#shared/utils.ts"

export const api = createClient(buildApiUrl("/"), {
  headers: getAuthHeaders,
})
