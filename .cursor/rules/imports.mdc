---
description: 
globs: 
alwaysApply: true
---
Imports should always be inserted in the following way:

- Third-party imports
- <EMPTY_LINE>
- Monorepo package imports (any import that starts with `@init`)
- <EMPTY_LINE>
- Imports relative to the app or package. If the project has tsconfig paths enabled, use the `~/` import.
