export const APP_NAME = "Init"
export const APP_ID = "init"

export const ACCOUNT_ROLES = ["member", "admin", "owner"] as const

export const LOCALES = ["en", "es"] as const
export const DEFAULT_LOCALE = "en" as const
