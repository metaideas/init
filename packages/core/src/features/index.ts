// This is the `core` package where you should add all the shared code
// functionality that is specific to the project (meaning no generic utilities),
// but not specific to any single app.

// As a rule of thumb, you should write the code inside a shared/feature folder,
// and when you need to use it in a different package, you move it here.

export function unusedFeature() {
  return "This is a feature in the core package"
}
