# packages/db

Apply these rules in `packages/db/**`. The repository standards in the root
[`AGENTS.md`](../../AGENTS.md) also apply.

- Use Drizzle.
- Use the shared prefixed-ID helper.
- Use non-conflicting four-letter ID prefixes.
- Use timestamps where appropriate.
