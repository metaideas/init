# ===========PROJECT===========
# Add specific project environment variables here

# Client-side variables (PUBLIC_ prefix for Vite)
PUBLIC_API_URL="http://localhost:3001"

# ===========PACKAGES===========
# Add environment variables for all the packages you are using in the project

# --- Tauri (@init/env/tauri) ---
# These are automatically set by Tauri at build/runtime, but can be overridden
# TAURI_ENV_DEBUG="false"
# TAURI_ENV_TARGET_TRIPLE=""
# TAURI_ENV_ARCH=""
# TAURI_ENV_PLATFORM=""
# TAURI_ENV_PLATFORM_VERSION=""
# TAURI_ENV_FAMILY=""

# ===========OPTIONAL===========
# Optional packages that may be used in the future

# --- Analytics / PostHog (optional) ---
# PUBLIC_POSTHOG_HOST=""
# PUBLIC_POSTHOG_API_KEY=""

# --- Observability / Sentry (optional) ---
# PUBLIC_SENTRY_DSN=""
# PUBLIC_SENTRY_DEBUG="false"
