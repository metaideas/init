{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PUBLIC_SITE_URL"],
      "outputs": ["dist/**"]
    },
    "typegen": {
      "env": ["PUBLIC_SITE_URL"]
    }
  }
}
