{
  "$schema": "https://v2-10-7.turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PUBLIC_SITE_URL"],
      "outputs": ["dist/**"]
    },
    "codegen": {
      "env": ["PUBLIC_SITE_URL"],
      "outputs": ["$TURBO_EXTENDS$", ".astro/**"]
    }
  }
}
