{
  "name": "api",
  "exports": {
    "./client": {
      "import": {
        "types": "./dist/client.d.ts"
      }
    }
  },
  "scripts": {
    "build:docker": "docker build -t init-api -f Dockerfile ../..",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "bump:deps": "bun update --interactive",
    "dev": "bun --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "typegen": "tsup src/client.ts --dts-only",
    "typegen:watch": "tsup src/client.ts --dts-only --watch"
  },
  "dependencies": {
    "@hono/trpc-server": "0.4.0",
    "@hono/zod-validator": "0.7.3",
    "@init/auth": "workspace:*",
    "@init/db": "workspace:*",
    "@init/env": "workspace:*",
    "@init/kv": "workspace:*",
    "@init/observability": "workspace:*",
    "@init/security": "workspace:*",
    "@init/utils": "workspace:*",
    "@trpc/server": "11.6.0",
    "hono": "4.9.8",
    "superjson": "2.2.2"
  },
  "devDependencies": {
    "@tooling/helpers": "workspace:*",
    "@tooling/tsconfig": "workspace:*",
    "@types/bun": "1.2.22",
    "tsup": "8.5.0",
    "typescript": "catalog:"
  }
}
