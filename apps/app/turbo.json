{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".output/**"],
      "env": ["PUBLIC_SENTRY_DEBUG", "PUBLIC_SENTRY_DSN"]
    }
  }
}
