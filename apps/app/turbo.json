{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "globalEnv": ["NEXT_PUBLIC_BASE_URL", "NEXT_PUBLIC_API_URL"],
  "tasks": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "next-env.d.ts",
        ".content-collections/**"
      ],
      "env": ["VERCEL_URL"]
    }
  }
}
