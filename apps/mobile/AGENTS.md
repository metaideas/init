# apps/mobile

Apply these rules in `apps/mobile/**`. The repository standards in the root
[`AGENTS.md`](../../AGENTS.md) also apply.

- Use functional React components.
- Use Expo APIs.
- Use Expo Router for navigation.
- Use Expo to handle assets.
- Use Reanimated for performance-sensitive animation.
