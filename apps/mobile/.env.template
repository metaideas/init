# ===========PROJECT===========
# Add specific project environment variables here

EXPO_PUBLIC_API_URL="localhost:3001"

# ===========PACKAGES===========
# Add environment variables for all the packages you are using in the project

# --- Analytics / PostHog (Expo) ---
EXPO_PUBLIC_POSTHOG_HOST="http://localhost:8079"
EXPO_PUBLIC_POSTHOG_API_KEY="dev-posthog-key"

# --- Observability / Sentry (Expo) ---
EXPO_PUBLIC_SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"
EXPO_PUBLIC_SENTRY_URL="https://sentry.io/"
EXPO_PUBLIC_SENTRY_ORG="dev-org"
EXPO_PUBLIC_SENTRY_PROJECT="mobile"
# These are example defaults; replace with your actual local/test values as needed.

# --- Sentry Auth Token (server/build only, not public) ---
# SENTRY_AUTH_TOKEN=""  # Only needed for build scripts, do NOT expose in the app!
