# ===========PROJECT===========
# Add specific project environment variables here

# Client-side variables (EXPO_PUBLIC_ prefix for Expo)
EXPO_PUBLIC_API_URL="http://localhost:3001"

# ===========PACKAGES===========
# Add environment variables for all the packages you are using in the project

# --- Observability / Sentry (@init/env/sentry/expo) ---
# Client-side (public)
EXPO_PUBLIC_SENTRY_DSN="https://public@example.invalid/1"
EXPO_PUBLIC_SENTRY_ORG="example-org"
EXPO_PUBLIC_SENTRY_PROJECT="example-project"
EXPO_PUBLIC_SENTRY_URL="https://example.invalid/"

# Server-side (build-time only, NOT exposed to the app)
SENTRY_AUTH_TOKEN="dev-sentry-auth-token"
SENTRY_ORG="example-org"
SENTRY_PROJECT="example-project"
SENTRY_DEBUG="false"

# ===========OPTIONAL===========
# Optional packages that may be used in the future

# --- Analytics / PostHog (Expo) ---
# EXPO_PUBLIC_POSTHOG_HOST=""
# EXPO_PUBLIC_POSTHOG_API_KEY=""
